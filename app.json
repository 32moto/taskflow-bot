{
  "name": "Taskflow bot by LINE Messaging API",
  "description": "This is a Taskflow bot by LINE Messaging API",
  "keywords": ["chatbot", "line", "go", "taskflow"],
  "env": {
    "LINE_BOT_CHANNEL_ACCESS_TOKEN": {
      "description": "LINE bot Channel access token from the Channel Console",
      "required":  true
    },
    "LINE_BOT_CHANNEL_SECRET": {
      "description": "LINE bot Channel secret from the Channel Console",
      "required":  true
    }
  },
  "stack": "container"
}
